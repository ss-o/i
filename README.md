
# `i`

## Usage

```sh
# Install <user>/<repo> from GitHub
curl https://get.digitalclouds.one/<user>/<repo>@<release>! | bash
```
```sh
# Run in docker 
docker run -d -p 3000:3000 --restart always ghcr.io/ss-o/i:main
curl http://localhost:3000/<user>/<repo>@<release>! | bash
```
